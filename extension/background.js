// Minimal service worker. Currently a no-op — all logic lives in popup.js.
// Reserved for future features (e.g. context menu, keyboard shortcut).
chrome.runtime.onInstalled.addListener(() => {
  // Nothing to do yet.
})
